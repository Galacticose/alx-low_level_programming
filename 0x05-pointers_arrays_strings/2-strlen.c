#include "main.h"
#include <string.h>

/**
 * _strlen - checks the lenght of a string
 * @s: stores a character value
 * @i: stores an integer value
 *
 * Return: Always i 
 */
int _strlen(char *s)
{
	int i;

	i = strlen(s);
	return (i);
}
