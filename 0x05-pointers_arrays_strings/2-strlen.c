#include "main.h"
#include <string.h>

/**
 * _strlen - checks the lenght of a string
 * @s: stores a character value
 *
 * Return: Always length of string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
