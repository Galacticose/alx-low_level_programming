#include "main.h"
#include <string.h>

/**
 * _strcmp - compare two strings
 * @s1: stores a character
 * @s2: stores a character
 *
 * Return: compared strings
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
